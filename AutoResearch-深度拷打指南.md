# AutoResearch 深度拷打指南

> 从「看得懂」到「讲得深」。
> 目标：面试官对这个项目往死里追问时，你能从容应答，甚至让他觉得"这学生有真东西"。
> 配合阅读顺序：先读《AutoResearch-新手教程.md》建立认知，再读这份深挖。

---

## 目录

- [第一章 项目的"亮点金字塔"：面试官会扑向哪里](#第一章-项目的亮点金字塔面试官会扑向哪里)
- [第二章 深挖①：结构化输出容错管线（含模型性能深挖）](#第二章-深挖①结构化输出容错管线含模型性能深挖)
- [第三章 深挖②：人机协同 HITL 与一致性保证](#第三章-深挖②人机协同-hitl-与一致性保证)
- [第四章 深挖③：Send 并行扇出与 reducer 状态合并](#第四章-深挖③send-并行扇出与-reducer-状态合并)
- [第五章 深挖④：Checkpointer 断点持久化与可靠性](#第五章-深挖④checkpointer-断点持久化与可靠性)
- [第六章 深挖⑤：SSE 事件流的工程细节](#第六章-深挖⑤sse-事件流的工程细节)
- [第七章 大模型性能专题：质量 / 速度 / 成本](#第七章-大模型性能专题质量--速度--成本)
- [第八章 未来改进路线图（面试加分必背）](#第八章-未来改进路线图面试加分必背)
- [第九章 终极拷打 30 问](#第九章-终极拷打-30-问)

---

## 第一章 项目的"亮点金字塔"：面试官会扑向哪里

### 1.1 一个残酷的事实

应届生简历上的项目，面试官心里默认都是"照着教程做的 Demo"。**他追问你，不是想听你背流程，而是想验证三件事**：

1. **你是真的写过这些代码，还是背的**（追问细节、为什么这么设计）；
2. **你有没有踩过坑、有没有工程思维**（问异常路径、问权衡取舍）；
3. **你对"大模型应用"这件事的理解深度**（问性能、成本、幻觉、评估）。

所以，你的项目陈述要像一颗**洋葱**：外层是流程，往里是设计动机，最核心是你踩过的坑和权衡。面试官每剥一层，你都有一层新东西给他看。

### 1.2 亮点金字塔（含金量从高到低）

| 等级 | 亮点 | 一句话定位 | 面试官可能的提问 |
|---|---|---|---|
| **S** | 结构化输出容错管线 | "模型不可靠，我用工程把它扛住了" | 为什么模型会输出乱格式？清洗怎么做的？重试几次？成本呢？ |
| **S** | 人机协同 + 一致性保证 | "AI 生成 + 人审，这是落地可信的钥匙" | interrupt 原理？为什么写回已确认内容？打回后内容相同怎么办？ |
| **A** | Send 并行扇出 + reducer | "我懂并行化，也懂它的坑" | reducer 是什么？空 Send 为什么致命？并行度怎么控制？ |
| **A** | Checkpointer 断点持久化 | "长任务能断点续跑，是可靠性的基本功" | 三种实现区别？SQLite 并发为什么有问题？进程重启怎么恢复？ |
| **A** | 逐级降级 + 兜底设计 | "我考虑过所有失败路径，不会让流程卡死烧钱" | 空任务兜底？失败自环？搜索全挂怎么办？ |
| **B** | SSE 事件流工程细节 | "前后端实时通信，我把边界情况做对了" | 历史快照防竞态？seq 去重？流式缓冲？ |
| **B** | 并发防双 resume 竞态 | "我懂 asyncio 并发模型的细节" | 为什么检查+标记间无 await 就能防并发？ |
| **B** | 多源搜索聚合降级 | "外部依赖不可控，我有兜底" | 为什么要两个搜索引擎？去重怎么做？ |
| **B** | 历史库独立设计 | "我理解了不同持久化的职责边界" | 为什么历史库要独立于 checkpoint？ |

### 1.3 你的整体叙事策略（面试回答的"元方法"）

不管被问到哪个点，记住这个三段式回答结构，**保证你不跑偏、不失分**：

> **① 问题（What/Why）**：我先讲清楚当时遇到什么问题、为什么这是个真问题；
> **② 方案（How）**：我的设计/代码是怎么解决的，关键取舍在哪；
> **③ 权衡与改进（Trade-off/Future）**：这个方案代价是什么、哪里还能更好。

面试官听出你有"权衡意识"和"改进意识"，就会从"考你"切换到"和你讨论"。**这是应届生能获得的最宝贵的面试状态。**

---

## 第二章 深挖①：结构化输出容错管线（含模型性能深挖）

> 这是你整个项目**最能打**的点。它是真实踩坑换来的，不是背出来的。本章值得你反复读 3 遍。

### 2.1 先讲清楚：什么是"结构化输出"？为什么非它不可？

大模型默认是"说人话"的——你问它，它给你一段自然语言。但程序里，我们常常需要模型输出**规整的数据**，方便直接变成对象、存库、处理。比如：

- 让规划师输出大纲 → 希望得到 `{"sections": ["行业概况", ...], "key_questions": ["市场规模多大?", ...]}`
- 让拆解师输出子任务 → 希望得到 `{"tasks": [{"id": "1", "description": "...", "keywords": [...]}]}`

如果模型输出的是一坨自由文本，程序还要自己解析、还可能解析错。所以**结构化输出**（让模型按指定 JSON 结构输出）是 LLM 应用开发的基本功。

**比喻**：你雇了个很有才华但有点马虎的实习生。你让他填一张《大纲申请表》（表格 = JSON schema）。靠谱的要求是：各栏别填错、别填成别的格式。但实习生偶尔会把"章节列表"这一栏填成一个"描述对象"而不是"列表"——格式乱了，后面的人没法处理。

### 2.2 问题全貌：为什么"正常方法"在这不灵了？

常规做法是 LangChain 的 `with_structured_output`——它会根据你的 Pydantic 模型生成约束，让模型"必须"按 schema 输出。强约束模式有两条路线：`function calling`（借用工具调用协议做结构化输出）和 `json_schema`（按 schema 约束输出），都要求模型原生支持。

项目里的基座模型是 `deepseek-v4-flash`（见 `backend/config.py:31`）。它**支持 function calling（工具调用）和 json_schema 强约束结构化输出**；但项目未把强约束模式用于结构化输出，而是选了 `json_object` 模式——**它只保证"输出的东西是合法 JSON"，不保证"JSON 的结构符合你的要求"**。这是**架构选择**（确定性 workflow，工具由代码直接调用），不是能力缺失，详见《为什么不用FunctionCalling》。

更糟的是（这是项目踩出来的真实规律，见 `docs/BUGS.md` #21）：**当 prompt 变长（比如打回大纲时把老板的修改意见拼了进去），模型越容易在格式上跑偏**——把"字符串数组"`sections` 输出成"对象数组"`[{"title": "行业概况"}]`。

**这一层要能讲出"为什么长 prompt 更容易跑偏"**，这是对大模型能力的理解：

- **指令遵循能力随上下文长度下降**：模型要在长上下文里"记住并遵守"你的格式要求，注意力会被分散，后面拼进去的修改意见和前面的格式约束"抢注意力"。
- **json_object 模式约束力弱**：它只约束"合法 JSON"，不约束"符合 schema"。模型自由发挥空间大。
- **模型能力分级**：flash 类模型（轻量、快、便宜）在指令遵循上天然弱于旗舰模型。**你选了便宜快的模型，就要为它的"不听话"买单——用工程兜住。**

### 2.3 解决方案：三层容错管线（逐层拆解）

项目没有傻等模型变听话，而是写了套容错管线（`backend/nodes.py:122`）。**这就是"把不可控的 AI 嵌进可控的工程"的教科书案例。** 我们逐层拆：

**第 0 层：调模型前——给 prompt 打补丁**
```python
if "json" not in prompt.lower():
    prompt += "\n请以 JSON 格式输出结果。"
```
为什么要这行？因为 DeepSeek 的 `json_object` 模式**要求 prompt 里出现 "json" 字样**才生效（这是模型侧的一个隐藏要求）。没这行，response_format 可能不生效。**这种"隐藏规则"只有真实调 API 才会发现**——讲出来就是"我是真调过"的证据。

**第 1 层：从文本里"抠"JSON —— `_extract_json`（nodes.py:152）**
模型输出的可能不是干净的 JSON，可能是：
- ```` ```json {...} ``` ````（被代码块包着）
- `好的，以下是大纲：{...}`（前后有废话）

处理策略（三级递进）：
1. 先剥掉 ```` ```json ```` 包裹，整体解析；
2. 整体解析失败 → 取"第一个 `{` 到最后一个 `}`"之间的子串再解析（容忍前后杂讯）；
3. 还失败 → 抛出**带原文片段**的异常，方便排查（`text[:200]`）。

**第 2 层：字段级清洗 —— `_coerce_model` + `_coerce_str_list`（nodes.py:58, 91）**
这是**最巧**的一层，专治"字符串数组变成对象数组"。核心逻辑（我用人话翻译）：

```python
对 sections 里的每一项：
    如果是字符串 → 直接收下
    如果是对象（比如 {"title": "行业概况"}）
        → 从对象里按优先级挑一个文本字段：
          title → name → description → content → text → label → tag
        → 挑出来当字符串用
```

为什么按这个优先级？因为**模型在"把数组元素转成对象"时，最常给这个字段起名叫 title 或 name**。这是项目在真实调试中观察到的规律（BUGS.md #21）。这个 `_coerce_str_list` 不是凭空写的，是**看了真实失败样本总结出来的**——面试时一定要强调这点："我的清洗规则不是拍脑袋，是跑了真实请求、看了模型真实输出的脏数据，总结出的启发式。"

对于嵌套结构（比如 `SubTaskList.tasks`，里面每个元素又是 `SubTask`），`_coerce_model` 会**递归**调用自己，逐层清洗。这是通用性设计。

**第 3 层：Pydantic 校验 + 重试 —— 循环 3 次（nodes.py:142）**
清洗完再用 Pydantic 模型校验（`Outline`/`SubTaskList` 就是 schema）。如果还是校验不过 → 重新生成一次（重试），最多 3 次。

### 2.4 深挖：为什么"重试 3 次"？代价是什么？

这是个很值得展开的点，面试官大概率会问"为什么不重试 10 次？"

**回答思路**：
- 每次重试 = 一次完整的 LLM 调用 = 一次完整的花费。3 次失败意味着最多 **4 次调用**（1 次初始 + 3 次重试）。
- 成功率曲线是**边际递减**的：第一次失败后，第二次成功的概率并不低；但如果连续 3 次都失败，说明大概率是"模型这一轮就是学不会这个格式"或"prompt 本身有问题"，再重试只是烧钱。
- 3 是一个**经验折中**：既给了模型多次机会，又不至于让单次节点调用成本失控。
- **改进空间**（主动说出，加分）：重试不是唯一的杠杆。更狠的做法是——重试时把上次失败原因（ValidationError 的信息）反馈给模型，让它在下次生成时"避免上次的错误"。这叫 **self-correction（自纠正）**，能显著提高第 2、3 次重试的成功率。项目目前没做这个反馈式重试，**这是个很自然、很专业的改进点**。

### 2.5 深挖：这套容错管线的"哲学定位"

面试官可能问："你搞这么复杂，为什么不直接换个更强的模型（启用 json_schema 强约束）？"

**这是个灵魂拷问，回答要专业**：

> 两方面都想过，这是**工程权衡**：
> ① 当前基座其实已支持 json_schema / function_calling 强约束，可以直接启用 `with_structured_output` 的强约束模式；换成更强的旗舰模型（如 GPT/Claude）表现更稳，但成本更高、响应更慢，而且**再强的模型在极端长 prompt 下也可能出格式问题**——容错管线是**永久需要的防御**，不是临时补丁。
> ② 在"当前基座（deepseek-v4-flash）已经够用"的前提下，花一次把容错管线写对，比持续为"更强的模型"付费更划算。
> ③ 正确姿势是**两手抓**：模型侧尽量选支持强约束的，工程侧永远保留容错兜底。我把 LLM 输出当"有噪声的信号"来对待，而不是假设它永远正确——**这是 LLM 应用开发和传统编程最大的思维差异**。

这段话能把面试官对你的印象从"调 API 的"拉高到"理解 LLM 工程本质的"。

### 2.6 本章拷打与应对（自我问答）

**Q：你如何验证这套容错管线真的有效？有没有数据？**
> 诚实版：没有单独做成功率定量统计，但项目用真实 LLM 的集成测试覆盖了"带修改意见的长 prompt"场景（`tests/test_stage2.py` 的 revise_outline / revise_draft 用例），跑通了"打回→重新生成→再次确认"全流程，0 报错。要补强的话，可以做一次"100 次生成、统计格式解析成功率"的基准测试。
> （这里"诚实 + 给出补强方案"比吹数据可信。）

**Q：清洗规则会不会把合法数据误伤？**
> 识别得很准：只有当 Pydantic 直接校验**失败**时，才进入清洗分支（`_coerce_model` 先 `model_validate`，失败才清洗）。所以清洗只在"原数据确实不合法"时触发，不会动合法数据。且清洗后仍要做一次 `model_validate`，清洗结果不合法照样失败重试——**多层防线互相兜底**。

---

## 第三章 深挖②：人机协同 HITL 与一致性保证

> 第二个 S 级亮点。核心就一句话：**AI 生成 + 人审 = 可信**。但难的是"怎么做对"。

### 3.1 interrupt 的本质：一个"审批点"

**比喻**：想象一条自动化流水线，但流水线上设了两个"审批窗口"。零件流到窗口，必须停下来，等旁边坐着的质检员（用户）看完、盖章（批准）或写批注（打回），流水线才继续。

项目里的两个审批点（`backend/nodes.py`）：
- **大纲确认**（规划师出大纲后）——`interrupt({"type": "plan", "outline": ...})`
- **草稿审核**（写手出草稿后）——`interrupt({"type": "draft", "report": ...})`

### 3.2 深挖：interrupt 的底层原理（这是懂不懂框架的分水岭）

**interrupt 不是"暂停个变量"，它的底层是抛异常。** 当节点执行到 `interrupt(payload)` 时，LangGraph 内部抛出一个 `GraphInterrupt` 异常，整个执行栈被"解开"，图停下来；同时 checkpointer 把状态存盘。**所以节点代码里必须单独处理这个异常**：

```python
try:
    ...
    resp = interrupt({...})      # 图在这里暂停
    ...
except GraphInterrupt:
    raise                        # nodes.py:311 —— 必须重新抛出！
except Exception as e:
    return {"error_log": [f"..."]}
```

**为什么必须 re-raise？** 如果 `GraphInterrupt` 被 `except Exception` 这个笼子吞掉了，图的暂停机制就失效了——它会以为自己"只是出了个错"，继续走错误的路径。**只有让它继续往上抛，LangGraph 才能正确感知"这是要暂停"。** 这是源码级理解，面试官问"为什么代码里有那么个 raise"时，能答出这一层就是加分。

### 3.3 深挖：恢复的两种姿势 —— update_state vs Command(resume)

恢复执行时，项目做了**两步**（`backend/manager.py` 的 feedback 方法）：

**第 1 步：`aupdate_state` 写回"人工确认过的内容"**
```python
await graph.aupdate_state(config, {"outline": Outline(**outline)})
```
**第 2 步：`Command(resume=...)` 恢复图**
```python
asyncio.create_task(self._execute(thread_id, Command(resume={"approved": ..., "feedback": ...}), config))
```

**为什么必须两步？** 这是最容易踩坑、也最能体现理解的地方：

- `update_state` 只负责**改状态字段**，它**不会让 interrupt 继续往下走**；
- `Command(resume=...)` 作为图的输入，才会让之前那个 `interrupt()` **返回值**，图继续跑；
- 两者必须配合。而且**顺序**很重要：先写回已确认内容，再恢复执行。

**为什么必须写回已确认内容？——一致性问题的核心**：

interrupt 恢复时，节点函数会**重新执行一遍**（重新进入 `supervisor_planner` 或 `writer`）。如果恢复时状态里没有"人工确认的版本"，节点会**基于旧逻辑重新生成一份新的大纲/草稿**——结果就是：**你审批看到的是 A 版，最后落盘的是 B 版，用户会骂你"我批的不是这个！"**。

所以代码里（nodes.py:292, 382）特意有这段逻辑：
```python
elif state.get("outline") is not None:
    # 恢复重跑：state 中已有人工确认的大纲（由 feedback 端点 update_state 写回）
    outline = state["outline"]      # 用确认版，不重新生成！
```
**一句话总结：人工确认过的内容，是这个流程的"事实来源"（source of truth）。** 这句话面试时背下来，非常提气。

### 3.4 深挖：打回后"内容没变"怎么办？—— seq 去重的由来

这是 BUGS.md #21 记录的严重 bug，值得深挖，因为它横跨前端 + 后端 + LLM 行为三块：

**场景**：老板打回草稿 → 模型按意见重写 → 但如果意见比较模糊（或模型低温下输出稳定），**重写出的草稿内容和上次一模一样**。

**前端原本的去重逻辑**是"按内容去重"：判断 `type + JSON.stringify(data)` 是否见过。于是这个"内容相同的新草稿"被误判成"断线重连重放的历史事件"，**直接忽略，确认面板不再弹出** → 前端永远等不到新确认框，用户以为系统坏了。

**解决**：后端给每条事件附一个**单调递增的 `seq` 序号**（`backend/manager.py:103`）。前端改为按序号去重：

```javascript
if (seq <= lastInterruptSeq) return;   // 旧事件（序号小）忽略
lastInterruptSeq = seq;                // 新事件（序号大）哪怕内容一样也处理
```

**这里体现的思维**：**"事件的身份"应该用"顺序"来标识，而不是"内容"**。内容可能巧合相同，但"这是新发生的"这件事，必须由系统给的唯一标识来证明。这是一个通用的分布式/事件系统设计原则，面试官听到这一层会知道你写过真东西。

### 3.5 本章拷打与应对

**Q：为什么要有两道人工确认？不是更麻烦了吗？**
> 因为**大模型最大的风险是幻觉**——一本正经地编造数据。在"大纲"（方向对不对）和"草稿"（数据对不对）两个关键决策点让人把关，是用**最小的人工成本**换来**最大的可信度**。而且这是 LLM 应用落地的共识：纯 AI 输出不可信，**AI 生成 + 人审**才是能交付给客户的形式。两道确认点之间是自动化，人工只在最关键处介入。

**Q：如果用户在审批点一直不操作，会怎样？**
> 图**暂停在 interrupt 处，不消耗任何 token**（没有 LLM 在跑）。checkpoint 已存档，用户关掉页面、明天再来，重新打开还能从原地继续。所以"等人"这件事对系统是无成本的。这也是设计成"暂停"而不是"轮询"的意义。

---

## 第四章 深挖③：Send 并行扇出与 reducer 状态合并

### 4.1 为什么需要 Send？—— 动态并行

**问题**：拆解师拆出的子任务数量是**运行时才知道的**（3 个、5 个、8 个，取决于模型）。静态图无法预先画好"第 1 个调查员、第 2 个调查员……"，因为不知道有几个。

**Send 就是 LangGraph 的"运行时动态派活"**：在一个节点里，根据数据生成任意多个"新分支"，每个分支从一个指定节点开始独立执行。

```python
# graph.py:72
def continue_to_researchers(state):
    sub_tasks = state.get("sub_tasks") or []
    if not sub_tasks:
        return "supervisor_planner"   # 兜底（见 4.4）
    return [Send("researcher", {"sub_task": t}) for t in sub_tasks]
```

3 个子任务 → 3 个 `Send("researcher", {...})` → 3 个 researcher 并行跑。

**比喻**：项目经理手上有 N 个活（N 运行时才定），他**现场**叫来 N 个临时工，每人领一个活，同时开工。而不是事先固定雇好 3 个人。

### 4.2 深挖：reducer —— 并行结果怎么"汇"？

**这是理解 LangGraph 状态系统的核心概念，面试必考。**

`backend/state.py:24`：
```python
search_results: Annotated[list, operator.add]
```

拆解这个语法：
- 默认情况下，多个节点往同一个 state 字段写值，是**后写覆盖**；
- `Annotated[T, reducer]` 的含义：这个字段的更新，要经过 `reducer` 这个函数处理；
- `operator.add` 对 list 来说 = **拼接**。所以 3 个 researcher 各返回 `[r1, r2]`，合并后是 6 条，而不是"最后一个覆盖前面的"。

**superstep（汇合点）**：LangGraph 会等**所有**扇出的分支都执行完，才继续往下走（到 analyst）。分析师拿到的，是**全部分支合并后的完整结果**。这也是为什么"analyst 只执行一次"——不是每个分支都触发一次。

**为什么要讲清楚这个？** 因为这是"并行执行 + 结果正确合并"能否成立的底层保证。面试官如果问"多个并行分支的结果你怎么汇总的"，你答"我用 `operator.add` reducer 让 LangGraph 自动拼接" + 讲一句"否则后写覆盖会导致数据丢失"，就是满分级回答。

### 4.3 深挖：空 Send 为什么致命？（bug 复盘）

这是 BUGS.md #13 记录的**严重 bug**，也是极佳的"踩坑故事"。

**现象**：拆解师拆解失败（返回空列表）时，前端永远转圈，没有结果也没有错误。

**根因**：如果 `continue_to_researchers` 返回**空的 Send 列表** `[]`，LangGraph 会认为"没有任何分支要跑"→ **图直接结束**。而"图正常结束"本来应该意味着"有 final_report"，可这里啥都没有。后端 `_execute` 的判断逻辑（有 interrupt？有 final_report？）**两个都不命中**，于是没有任何事件推给前端 → 前端永久等待。

**解决（两层兜底）**：
1. 空 `sub_tasks` 时**不返回空 Send**，而是返回 `"supervisor_planner"`（回规划节点重新规划）——`graph.py:81`；
2. 后端在"图结束但既无中断又无报告"时，兜底推一个 `error` 事件（manager.py:274），保证前端**至少能看到明确错误**而不是死等。

**这个 bug 的价值**：它证明了"边角失败路径"在 AI 系统里尤其要命——因为触发条件是不可控的（模型拆解失败），且失败表现是**静默**的（不报错，只是卡住）。**面试时讲这个 bug，展示的是你的"异常路径意识"。**

### 4.4 本章拷打与应对

**Q：并行搜索会不会有并发上限？子任务很多怎么办？**
> 当前子任务数受模型拆解倾向约束，一般个位数。真到几十个会撞 LLM/搜索 API 的并发配额。改进方案：① 拆解时限制子任务数量上限；② 用信号量（Semaphore）给并行度设闸；③ 按批扇出（先跑前 N 个，做完再跑下一批）。这是面试加分点，主动抛出。

**Q：如果两个子任务的搜索结果互相依赖怎么办？**
> 当前子任务是按大纲章节/关键问题切分的**独立**任务，天然无依赖。如果未来有依赖，可以：① 在同一个 researcher 里串行完成依赖步骤；② 拆成多阶段图（第一层搜 A，第二层基于 A 搜 B）。用图的多层结构表达依赖。

---

## 第五章 深挖④：Checkpointer 断点持久化与可靠性

### 5.1 三种存档方式：memory / sqlite / postgres

`backend/graph.py:149` 根据配置创建 checkpointer。**面试官很可能问"三种的区别和选择"，你要能答出每一层的权衡**：

| 实现 | 存储位置 | 可靠性 | 适用场景 | 代价 |
|---|---|---|---|---|
| `MemorySaver` | 进程内存 | 重启即失 | 测试、单进程开发 | 零依赖 |
| `AsyncSqliteSaver` | 本地文件 | 进程重启不丢 | 单实例、中小规模 | 并发写有限 |
| `AsyncPostgresSaver` | PostgreSQL | 高可靠、支持多实例 | 生产（Docker 部署） | 要起数据库 |

**为什么生产用 PostgreSQL？** 项目实际踩过 SQLite 的坑（BUGS.md #8）：**SQLite 并发写**会导致 `.sqlite-wal` / `.sqlite-shm` 残留损坏，`AsyncSqliteSaver` 直接**无限挂起**。多进程并发写同一个检查点文件，是 SQLite 的天然短板。PostgreSQL 作为真正的服务端数据库，并发写安全得多。

**为什么把"测试用独立临时库 + 清 wal 残留"写进测试代码？** 因为开发时反复跑集成测试，把开发库的 checkpoint 文件写坏了（BUGS.md #8）。这个细节讲出来，说明你"测试会污染环境"这种工程意识都有。

### 5.2 深挖：序列化问题 —— Pydantic 模型怎么存进 checkpoint？

这是 BUGS.md #4 的坑：state 里存了 Pydantic 对象（Outline/SubTask），langgraph 的 msgpack 序列化器**默认不允许反序列化未注册模块的类型**，当前版本是告警、未来版本会**直接报错**。

解决（`graph.py:134`）：
```python
JsonPlusSerializer(
    allowed_msgpack_modules=[
        ("models", "Outline"),
        ("models", "SubTask"),
        ("models", "SubTaskList"),
    ]
)
```
显式声明"允许反序列化这几个类型"。**能讲到"我为了往前兼容，把类型白名单配置进序列化器"，说明你不是停留在"能用"，而是注意了"未来版本不崩"。**

### 5.3 深挖：进程重启后怎么恢复？—— restore 机制

场景：调研暂停在大纲审批点，服务器重启了。`threads` 字典是**纯内存**的（`manager.py`），重启后为空。前端刷新页面重新连 SSE，服务端怎么知道这个调研？

**restore 流程**（`manager.py:313`）：
1. SSE 订阅时发现 thread 不在内存 → 调 `restore(thread_id)`；
2. 从 checkpoint `aget_state` 读该线程的最新状态；
3. 用 `_interrupt_info`（manager.py:54）判断它停在哪：
   - 停在大纲/草稿确认点 → 重建 ThreadChannel，补发 `node_start` + `interrupt` 事件 → 前端能继续审批；
   - 已完成 → 补发 `final` 事件重放报告；
   - 其他（意外终止）→ 补发 `error`。
4. 恢复成功就把线程放回内存，继续用。

**`_interrupt_info` 的一个细节**（manager.py:54）：它遍历 `st.tasks` 找**第一个**带 interrupts 的任务，而不是简单取最后一个。注释说明了原因：避免理论上的多分支场景取错。**这种"注释里写清楚为什么不用更简单的写法"的地方，就是面试时能挖出来的真细节。**

### 5.4 本章拷打与应对

**Q：checkpoint 和历史库都存东西，会不会重复？**
> 职责不同：checkpoint 存**图运行状态**（为了恢复执行），历史库存**业务记录**（为了追溯和展示）。checkpoint 只保最新状态且 feedback 会被消费掉；历史库专门记录每次批改意见和最终报告。**一个为"运行"服务，一个为"业务"服务**，互不替代。（详见第 8 章 5.3 的历史库独立设计。）

---

## 第六章 深挖⑤：SSE 事件流的工程细节

### 6.1 SSE 是什么？为什么用它？

**SSE（Server-Sent Events）**：服务器→网页的**单向**实时推送。对比 WebSocket（双向）。**比喻：电台广播**——服务器一直播，网页打开就能听，但不能对着广播说话（要发指令走普通 POST 接口）。

项目场景是**单向的**（服务器推进度给前端看），SSE 恰好合适，而且 EventSource（浏览器原生 API）自带**断线自动重连**，不用自己写心跳。

### 6.2 深挖：事件历史 vs 实时推送的竞态（这个 bug 很高级）

需求：用户刷新页面后，SSE 重连，应该**先看到之前所有事件（历史补发），再接收新事件（实时推送）**。

**naive 实现**：新连接来了 → `for evt in history: 推给用户` → 然后进入实时推送循环。

**问题**：如果"历史补发"和"实时推送"是两个路径，边界没串行化，就会发生：
- 补发历史的过程中，新事件同时进了"历史"（会被补发一次）和"实时通道"（又被推一次）→ **重复**；
- 或者实时事件先到了，历史还没补完 → 顺序错乱。

**项目解法（很巧妙）**：把"复制历史"这个操作设计成**同步方法**（`subscribe()`，`manager.py:84`）。因为 Python 单线程 asyncio 中，**同步代码执行期间不会让出事件循环**，所以"复制历史快照"和"之后 publish 实时事件"之间**不可能交错**：

```python
def subscribe(self):
    q = asyncio.Queue()
    self._subscribers.append(q)
    for evt in self.history:      # 同步复制历史（这一整段不会被打断）
        q.put_nowait(evt)
    return q                      # 之后 publish 的实时事件，排在历史后面
```

**这是对 asyncio 单线程协作式调度模型的精确利用**。能讲清楚这层，面试官会对你另眼相看。

### 6.3 深挖：seq 去重（前面讲过，这里强调它的通用价值）

前端"按内容去重"会误伤"打回后内容相同的场景"（第 3.4 节）。解决是后端给事件加单调递增序号，前端按序号去重。**通用原则：事件的唯一身份应该由系统的顺序标识（seq）决定，而不是内容。** 这个原则在事件驱动系统里放之四海皆准。

### 6.4 深挖：流式输出为什么要"攒一攒"再推？

模型是一个字一个字生成的（token 流）。如果每生成一个字就推一条 SSE 消息，消息量爆炸、前端渲染也扛不住。项目用两个阈值**聚合**（`manager.py:193`）：
- 攒够 **80 个字符**，或
- 距上次推送超过 **0.3 秒**，
- 才推一条 `stream` 事件。

**权衡**：阈值小了→更实时但消息更碎；阈值大了→消息更少但打字机效果变"一卡一卡"。80 字符 / 0.3 秒是实测手感均衡值。**主动讲出"这个阈值是调过的"比给个数字更可信。**

### 6.5 深挖：一个让连接直接崩掉的 bug —— Pydantic 对象序列化

BUGS.md #6：resume 后 `supervisor_planner` 重跑返回的 `node_end` 事件里，`outline` 是 **Pydantic 对象**。SSE 生成器里 `json.dumps(evt)` 抛 `TypeError: Object of type Outline is not JSON serializable` → **整个 SSE 响应生成器异常退出，连接中断，后面所有事件全丢**。

解决：`_to_jsonable`（`manager.py:31`）递归把 `BaseModel → model_dump()`、dict/list 递归转换，确保所有事件可 JSON 序列化。

**这个 bug 的价值**：它说明"LLM/框架返回的对象"和"JSON 能序列化的东西"是两码事，任何 AI 应用后端都会撞上这类"对象不能直接 json.dumps"的坑。你能讲出"我踩过，我加了个递归转换器"，很真实。

### 6.6 本章拷打与应对

**Q：SSE 和 WebSocket 你什么时候选哪个？**
> 单向推送（服务器→客户端）用 SSE：更轻、EventSource 自带重连、不用处理复杂的握手和心跳。双向实时交互（聊天、协同编辑）用 WebSocket。本项目是"看进度"，单向，SSE 合适。

---

## 第七章 大模型性能专题：质量 / 速度 / 成本

> 这是你明确要求重点展开的部分。**"大模型性能"在面试语境里，通常不是指"模型跑得多快"，而是指一个系统工程的三维指标：质量（Quality）、速度（Latency）、成本（Cost）。** 面试官问"性能"，很可能是问这三者你怎么权衡、怎么优化。**这一章务必吃透。**

### 7.1 先建立框架：性能 = 质量 × 速度 × 成本

**比喻**：你雇了一个"超级实习生"（大模型）。你要从三个维度评估这笔雇佣值不值：
- **质量**：他写的报告对不对、格式准不准、会不会瞎编数据？（模型输出质量 / 幻觉 / 稳定性）
- **速度**：他写得快不快？你在旁边等多久？（延迟 / 吞吐）
- **成本**：他开价多少？按字收费的话，你这份报告要花多少钱？（token 消耗 / 调用费）

**这三者互相牵制**：要更高质量 → 换更强模型 → 更贵更慢；要更快更便宜 → 用 flash 小模型 → 质量下降、格式不稳。**所有 LLM 应用的性能优化，本质都是在三维里找平衡点。** 你的项目恰好展示了这套平衡术，这是面试里能聊得很深的话题。

### 7.2 质量维度：幻觉 / 稳定性 / 上下文利用

#### 7.2.1 幻觉（模型编数据）—— 项目怎么应对？

大模型不知道实时信息，还可能**编造看起来合理的假数据**（比如编一个"据 X 报告，市场规模 500 亿"）。项目的五层防线：

1. **联网搜索工具喂真实数据**：调查员用 `search_web` 搜索真实网页，分析师基于真实搜索摘要提取数据，而不是凭模型记忆编造；
2. **prompt 明确禁止编造**：分析师指令里写了"**无法确定的项请标注为 null 而不是编造**"（`nodes.py:228`）；
3. **注入当前时间**：`_time_hint` 让模型知道"现在"是哪天，避免用过时/过期数据；
4. **两道人工确认**：大纲和草稿让人把关，人能看到数据可疑之处；
5. **报告中注明来源**：writer 指令要求"涉及具体数字时注明数据来源或标注估算"（`nodes.py:251`）。

**面试话术**：单一手段挡不住幻觉，**要组合拳**——真实数据输入 + 输出约束 + 人审 + 来源可追溯。

#### 7.2.2 格式稳定性 —— 前面讲过，这里给个"认知升级"

模型输出格式不稳定的根因，是**模型是概率系统**，不是确定性程序。同一个 prompt 两次调用，输出可能有细微差别；上下文越长，约束遵守越弱。

**认知升级**：把 LLM 当作"**有噪声的信号源**"来设计系统，而不是当作"确定性的函数"。这就是为什么你要在它下游做校验、清洗、重试。**这个认知，是"用 LLM 的人"和"调 LLM 的 API 的人"的分界线。**

#### 7.2.3 上下文利用 —— 模型"眼前"能看多少？

**比喻**：模型的工作记忆像**一张桌子**。桌子（上下文窗口）能放的东西有限，你放太多，模型容易"顾此失彼"（注意力稀释），而且**桌子越大越贵**（token 按量计费）。

项目里的上下文管理（很典型）：
- `_MAX_RESULTS_FOR_LLM = 8`（`nodes.py:32`）：分析师只拿**前 8 条**搜索结果给模型，而不是全部。为什么？搜索可能有 10+ 条结果，全塞进去 prompt 太长、又贵又容易稀释注意力；
- `extracted_data` 传给 writer 时**截断到 6000 字符**（`nodes.py:249`）：提炼后的数据可能很大，截断保住核心；
- 打回后的反馈会拼进 prompt，但**不重复**把整个历史塞进去。

**面试话术**："上下文管理 = 在'信息完整性'和'成本/注意力'之间取舍。该省的 token 要省，因为又贵又伤质量。"

### 7.3 速度（延迟）维度：全流程延迟拆解与优化

**面试官可能问"你这个调研一次要跑多久？慢在哪？怎么优化？"**

#### 7.3.1 延迟拆解（happy path）

| 步骤 | 大概耗时 | 瓶颈类型 |
|---|---|---|
| 规划师生成大纲 | 1~3 秒 | LLM 生成延迟 |
| （人工确认，不计入系统延迟） | 用户操作 | — |
| 拆解师拆任务 | 1~3 秒 | LLM 生成延迟 |
| 并行搜索 ×N | 2~5 秒 | 网络 / 搜索 API（**并行，不叠加**） |
| 分析师汇总 | 2~5 秒 | LLM 生成（**喂了 N 条结果，是较大的单次调用**） |
| 写手写草稿 | 10~30 秒 | **最慢**，生成整篇报告 |
| （人工审核） | 用户操作 | — |
| 定稿 | 1~3 秒 | LLM 生成 |

**结论**：全流程自动部分约 20~50 秒，**大头在"写手生成整篇草稿"**。

#### 7.3.2 项目的速度优化手段

1. **Send 并行搜索**：N 个子任务并行，搜索延迟不叠加（这是最大的速度优化）；
2. **SSE 流式输出**：模型一个字一个字地生成，前端**一边生成一边显示**（打字机效果）。用户感知到的"首屏延迟"从"等完整篇报告"变成"等第一个字"，**体感快了非常多**。这是大模型应用体验优化的经典手法；
3. **异步架构**：`start` 立即返回 thread_id，图在后台跑，前端不阻塞。

#### 7.3.3 未来能怎么更快（面试加分）

- **模型分级路由**：简单任务（拆解、定稿）用更便宜更快的 flash 模型，复杂任务（写草稿）才用旗舰模型。**不是所有节点都需要最强的模型**；
- **结果缓存**：同样的子任务搜索/生成结果可以缓存（比如同一主题调研第二次跑，直接复用）；
- **草稿生成分段并行**：把报告按章节分给多个 writer 并行写，再合并（类似 Send 的做法），可大幅缩短最慢环节；
- **prompt 缓存**：某些平台支持"相同前缀的 prompt 缓存"，打回重试时前缀相同可省钱省时。

### 7.4 成本（token）维度：给这个项目算一笔账

**面试官可能问"你这个项目一次调研烧多少 token / 多少钱？"** 你要会**算账**。

#### 7.4.1 调用次数账本（happy path）

| 节点 | LLM 调用次数 |
|---|---|
| 规划师 | 1 |
| 拆解师 | 1 |
| 调查员×N | 0（纯搜索，不调 LLM） |
| 分析师 | 1 |
| 写手 | 1 |
| 定稿师 | 1 |
| **合计** | **4 次 LLM 调用** + N 次搜索 |

**但真实成本不止这些**，要算上：
- **重试**：每节点最多 3 次重试 = 最多 4 次调用；
- **打回循环**：老板打回大纲 → 规划师重跑 1 次；打回草稿 → 分析师 + 写手各重跑 1 次；
- **失败自环兜底**：好在项目有兜底（见下），不会无限循环烧钱。

#### 7.4.2 项目的"省钱"设计（逐条，都是面试点）

1. **草稿反馈分流**（`graph.py:67`）：打回草稿时，**数据类反馈回分析师（重新提取），措辞类只回写手（直接重写）**。如果所有反馈都回分析师，会多一次"重新汇总分析"的昂贵调用。**用关键词启发式，省的是最贵的那一步**；
2. **失败自环保护**（`graph.py:64`）：writer/analyst 连续失败且无人工反馈 → 直接用现有草稿定稿，**切断死循环**。为什么重要？原来会一直路由回自己重试，每轮都调 LLM，直到 recursion_limit（默认 25 次）才停——**白烧 25 次调用费**。这个兜底一次修复"无反馈死循环烧钱"；
3. **上下文截断**：8 条结果、6000 字符截断，控制单次 prompt 长度；
4. **暂停不烧钱**：interrupt 期间图停着，0 token。

#### 7.4.3 成本控制的未来方向

- **模型路由**：小任务用小模型（省 10 倍单价）；
- **搜索缓存**：同主题多次调研，搜索/分析结果可复用；
- **prompt 缓存**：利用服务商的前缀缓存，打回重试时省钱；
- **异步/批处理**：低优先级任务排队错峰跑。

### 7.5 上下文工程（Prompt Engineering 的高阶版）

**认知升级**：Prompt Engineering 不只是"写提示词"，而是**"上下文工程"**——决定每一轮调用，模型"眼前"应该放什么、不放大什么。

项目里的上下文决策：
- 分析师：放"主题 + 大纲 + 前 8 条搜索结果"，不放全部；
- 写手：放"大纲 + 提炼后的数据（截断 6000 字）"，**不放原始搜索结果**（原始数据太大）；
- 打回重试：放"反馈意见"，**不放历史对话**（对话式记忆会无限膨胀）。

**面试话术**："上下文工程的核心是'只给模型完成当前任务所需的最小信息'。信息不足→质量差；信息过剩→又贵又稀释注意力。我在每个节点都重新设计'眼前放什么'。"

### 7.6 评估维度：怎么知道报告好不好？（诚实的短板 + 补强）

**这是项目最诚实的短板，也是面试官大概率会戳的地方：项目没有自动评估报告质量的机制。** 你要**主动**面对：

**当前靠什么**：
- 集成测试断言结构（有 outline、有 draft、无 error_log）；
- 两道人工确认（人在把关质量）；
- prompt 约束（null 不编造、标注来源）。

**未来怎么补**（主动提出，展示成长性）：
- **LLM-as-Judge**：用一个独立的强模型给报告打分（维度：准确性、完整性、结构、来源引用），自动化质量把关；
- **评测集（Golden Set）**：准备一批"已知答案"的调研主题，跑完自动比对，回归测试防止质量退化；
- **事实核查节点**：报告里的每个数字，回搜索引擎验证来源是否真实（防幻觉的自动化补充）；
- **用户反馈回收**：把用户的打回意见结构化，统计高频问题，反哺 prompt。

> 面试官问"你怎么知道你的报告是对的"，你答"**诚实：目前靠人审和结构测试，没有自动质量评估——但我知道怎么补（LLM-as-Judge + 评测集），也清楚为什么该补**"，这种诚实 + 方案比硬吹高到不知道哪里去。

---

## 第八章 未来改进路线图（面试加分必背）

> 面试官几乎必问："如果给你更多时间，你会怎么改进？" **提前备好，分维度讲，每个改进给出"是什么 / 为什么 / 怎么做 / 优先级"。** 讲的时候挑 2~3 个重点展开，别全背。

### 8.1 能力增强方向

**① 反思 / 自评估节点（最高优先级，也最好讲）**
- 是什么：在写手之后、人审之前，加一个"评估节点"——让模型/另一个模型先自我审查草稿（有没有数据矛盾、有没有遗漏章节、引用是否真实），不满意就自己重写。
- 为什么：人审成本高，先用 AI 自审"过一道"，把明显的质量问题在到达人之前就解决。
- 怎么做：LangGraph 加一个 `add_conditional_edges` 自环（"评估不过 → 回 writer 重写"）。这就是经典的 **Reflection / Self-correction 模式**。
- 优先级：高。这也是从"workflow"迈向"agentic"的关键一步。

**② 引用溯源到段落级**
- 是什么：报告中每个数据/论断，标注它来自哪条搜索结果。
- 为什么：可信度 + 可复核，缓解幻觉。
- 怎么做：analyst 提取数据时带上 source URL；writer 写报告时把来源标注进 markdown。

**③ 多轮追问式调研**
- 是什么：用户看了草稿后，不是只能"批准/打回"，还能追问"再深挖一下某某竞品"。
- 为什么：让"调研"从一次性变成对话式。
- 怎么做：把用户的追问当作新反馈，动态追加子任务。

**④ 长期记忆**
- 是什么：跨调研记住"用户偏好"（比如用户喜欢表格、喜欢市场规模开头）。
- 怎么做：存用户画像，注入每个节点的 prompt。

### 8.2 性能优化方向

**⑤ 模型分级路由**
- 是什么：简单节点（拆解、定稿）用 flash 小模型，复杂节点（写草稿）用旗舰模型。
- 为什么：省钱（小模型单价低 10 倍）+ 提速，且质量不掉（简单任务小模型够用）。
- 怎么做：`build_llm` 已经支持按 provider 切换，扩展成"按节点选模型"即可（`config.py` 里加个 model 选择函数）。

**⑥ 搜索与分析缓存**
- 是什么：同主题/近似主题的调研，缓存搜索结果和提炼数据。
- 为什么：省搜索 API 调用 + LLM 调用，第二次跑近乎免费。
- 怎么做：以 topic 的 hash 为 key 存缓存，命中直接复用。

**⑦ 草稿分段并行生成**
- 是什么：把报告按章节拆给多个 writer 并行写，再合并。
- 为什么：写草稿是全流程最慢环节（10~30 秒），分段并行可大幅缩短。
- 怎么做：类似 Send 的做法，按 section 扇出 writer 分支。代价：需要设计"章节衔接"（风格一致性）。

### 8.3 可靠性方向

**⑧ 重试退避 + 熔断**
- 是什么：LLM/搜索 API 调用失败时，指数退避重试；连续失败触发熔断，短时间不再请求该 Provider。
- 为什么：避免雪崩，保护上游，也保护钱包。
- 怎么做：`build_llm` 的 `max_retries` 目前是固定的 2，可升级为可配置的退避策略。

**⑨ 监控告警 + 可观测性**
- 是什么：接入 LangSmith（`config.py` 已预留 `LANGCHAIN_TRACING_V2`），记录每个节点的耗时、token、错误率。
- 为什么：出了问题能快速定位是哪个节点、哪个 Provider、花了多少钱。
- 怎么做：把预留的 tracing 开关打开，加自定义指标（节点耗时、token 用量、解析失败率）。

**⑩ 超时与资源控制**
- 是什么：给每个 LLM/搜索调用设超时上限；对并行扇出数、单任务 token 设上限。
- 为什么：防止一个异常任务拖垮整个流程或账单失控。

### 8.4 产品化方向

**⑪ 多用户 + 鉴权 + 配额**
- 是什么：当前是单用户 Demo（CORS 全开），生产要加用户体系、权限、用量配额。
- 为什么：能对外开放、能防滥用（别人蹭你的 API key 烧你钱）。
- 怎么做：加登录/Token，配额中间件；CORS 从 `*` 收紧（`main.py:78` 注释已提示）。

**⑫ 报告模板自定义 + 数据可视化**
- 是什么：用户能选模板（篇幅、风格、是否要图表）；数据自动生成图表。
- 怎么做：模板参数注入 prompt；用 matplotlib/图表库把 extracted_data 可视化，嵌进报告。

### 8.5 架构方向

**⑬ 消息队列解耦**
- 是什么：把"启动调研"做成异步任务队列（如 Redis 队列 / Celery），而不是进程内 `asyncio.create_task`。
- 为什么：进程内任务在重启/崩溃时丢失，且多实例部署时任务分布不均。
- 怎么做：调研任务发队列，worker 消费；配合 Postgres checkpoint 天然支持多 worker。

**⑭ 状态机独立于 LangGraph**
- 是什么：把业务状态（进行中/等待审批/已完成）的管理从 LangGraph 内部抽出来，用独立状态机表达。
- 为什么：业务状态和框架绑定太深，未来换编排框架代价大。
- 怎么做：`history.py` 里的 status 字段已经是个雏形，可扩展成完整状态机。

> **怎么挑重点讲**：面试时别把上面全倒出来。选 **①反思自评估、⑤模型路由、⑨可观测性** 三个展开（覆盖面：能力+性能+可靠性），其余一句带过"还有 XX 方向我也想过"。

---

## 第九章 终极拷打 30 问

> 最后这 30 问，是"考得最深"级别。每题先想 30 秒再对答案。答案要点已经写在括号里，你要能用自己的话讲出来。

### A. 项目总览与动机

**1. 用一句话说清这个项目是什么？**
（一个让 LLM 按"带人工审批点的流程图"自动完成市场调研的系统——用 LangGraph 编排多 Agent，两道人工确认保证可信，checkpoint 断点续跑。）

**2. 为什么不用一次性 prompt 让模型直接出报告？**
（幻觉 + 不可控 + 无法中途干预 + 无法复用。拆成多步骤 + 人工把关，才能交付可信结果。）

**3. 这个项目最让你骄傲的一个设计是什么？为什么？**
（结构化输出容错管线。因为它不是照搬教程，是真实踩坑后自己总结的"模型不可靠就靠工程兜"的方法论。）

### B. LangGraph 与状态

**4. State 里的 reducer 是什么？为什么 search_results 要用 operator.add？**
（`Annotated[list, operator.add]`——多节点写同字段时按 reducer 合并而非覆盖。并行 researcher 的结果要拼到同一个列表，覆盖会丢数据。）

**5. 节点函数的返回值是怎么进 State 的？**
（节点接收整个 state，返回"想更新的字段"dict；LangGraph 按每个字段的 reducer 合并进全局状态。）

**6. 为什么会有空 Send 静默终止的问题？你是怎么兜底的？**
（空 Send 列表 = 没有分支要跑 = 图直接结束但无任何产出。兜底：空任务返回"回规划节点重规划"，再加"无中断无报告则推 error 事件"。）

**7. 条件边和普通边的区别？举你项目里的例子。**
（普通边固定走；条件边由一个函数根据 state 决定走哪条。如 route_after_plan 看 plan_approved 决定去拆解还是回规划。）

### C. 人机协同

**8. interrupt 的底层原理？**
（内部抛 GraphInterrupt 异常暂停图 + checkpoint 落盘；恢复时用 Command(resume) 让 interrupt() 返回值。）

**9. 为什么节点里要单独 catch 并 re-raise GraphInterrupt？**
（否则被 except Exception 吞掉，暂停机制失效，图走错误路径。）

**10. update_state 和 Command(resume) 分别干什么？为什么都要？**
（update_state 只改字段不消费中断；Command(resume) 才让图继续。先写回人工确认版本保证一致性，再 resume。）

**11. 为什么恢复时要写回已确认的大纲/草稿？**
（恢复后节点会重跑，不写回会重新生成一份和人工看到不一致的内容。人工确认版 = 事实来源。）

**12. 打回后如果模型生成的还是一样的内容怎么办？**
（低温下内容可能完全相同。前端不能按内容去重，要按后端发的单调递增 seq 去重。）

### D. 持久化与可靠性

**13. 三种 checkpointer 的区别？生产用哪个？为什么？**
（memory 内存/sqlite 文件/postgres 数据库；生产用 postgres，规避 sqlite 并发写损坏问题。）

**14. 进程重启后暂停的调研怎么恢复？**
（restore：从 checkpoint 读状态，据 _interrupt_info 判断停在哪，补发 interrupt/final/error 事件，前端可继续。）

**15. 历史库为什么独立于 checkpoint？**
（checkpoint 保留每步状态快照但项目只读最新状态，且 feedback 被消费，不是可查询的业务档案；历史库存每次批改意见和报告全文，为了追溯展示，且不依赖 checkpoint 存活。）

**16. 失败自环问题是什么？怎么解决？**
（writer/analyst 失败但路由只看 approved，会无限重试烧钱直到 recursion_limit。兜底：最近错误来自 writer/analyst 且无反馈→直接定稿。）

**17. 搜索两个源都挂了怎么办？**
（返回 [MOCK] 模拟结果，流程不断。逐级降级：单源失败跳单源，全挂回 mock。）

### E. LLM 工程化

**18. 为什么不用 with_structured_output？**
（基座 deepseek-v4-flash 支持 function calling 与 json_schema 强约束模式，但未用它们做结构化输出是架构选择；项目用 json_object；且长 prompt 下格式不稳。改手写容错管线。）

**19. 容错管线的每一步是干什么的？**
（抠 JSON（_extract_json）→ 字段级清洗（对象数组转字符串数组）→ Pydantic 校验 → 失败重试 3 次。）

**20. 重试 3 次够吗？为什么不是 10 次？**
（成功率边际递减 + 每次重试是完整调用费。3 次是经验折中；可改进为"重试时把上次失败原因反馈给模型"的自纠正。）

**21. 你怎么衡量这套管线的效果？**
（诚实：集成测试覆盖打回长 prompt 场景跑通 0 报错；未来补"100 次生成统计解析成功率"基准测试。）

**22. 模型的幻觉你怎么防？**
（组合拳：真实搜索数据输入 + prompt 禁止编造(null) + 注入当前时间 + 两道人工确认 + 报告注明来源。）

### F. 大模型性能

**23. 这个项目一次调研要跑多久？慢在哪？**
（自动部分约 20~50 秒，大头在写手生成整篇草稿。并行搜索 + SSE 流式降低了体感延迟。）

**24. 怎么控制 token 成本？**
（草稿反馈分流省分析师调用、失败自环保护防无限烧钱、8 条结果/6000 字符截断、interrupt 暂停不耗 token。）

**25. 如果让你优化延迟，怎么做？**
（模型分级路由、草稿分段并行、结果缓存、prompt 缓存、流式输出。）

**26. 你对大模型"性能"怎么理解？**
（三维框架：质量（幻觉/稳定性）× 速度（延迟）× 成本（token）。三者牵制，LLM 应用性能优化=在这三维找平衡。）

### G. 综合与态度

**27. 你这个算"多 Agent"吗？**
（诚实+专业：算"由 LLM 驱动的确定性编排工作流 + 人机协同"，不是开放自由对话的多 Agent。区分清楚 workflow 和 agent，讲清为什么选 workflow（可控、可审、成本可预期），并展示我也懂怎么往 agent 演进（反思自评估节点）。）

**28. 如果换更强的模型，你的代码要改什么？**
（几乎不用改——统一走 OpenAI 兼容接口，改 .env 即可。容错管线保留，更强模型输出更稳，容错成本更低，属于冗余防御。）

**29. 这个项目最大的短板是什么？**
（诚实：没有自动质量评估；结构化输出靠工程兜而不是从模型侧根治；单用户无鉴权。每条都补上"我知道怎么改"。）

**30. 一句话总结你从项目里学到的最重要的一件事？**
（"LLM 是概率系统，不是确定性函数。做 LLM 应用的核心能力，是设计一套让不可控的模型在可控工程里稳定工作的系统——从状态管理、异常兜底、人机协同、成本控制到质量评估。"）

---

## 附：面试前速记（最后 10 分钟扫一眼）

- **项目一句话**：让 LLM 按带两道人工审批点的流程图自动完成市场调研，LangGraph 编排，checkpoint 断点续跑。
- **三个最硬的点**：结构化输出容错管线 / 人机协同一致性 / 逐级降级兜底。
- **两个必背概念**：reducer（`Annotated[list, operator.add]` 并行合并）；interrupt（GraphInterrupt 异常 + Command(resume)）。
- **两个必背 bug**：空 Send 静默终止 → 回规划兜底；失败自环烧钱 → 无反馈直接定稿。
- **性能三字诀**：质量（幻觉/格式）× 速度（延迟）× 成本（token），永远讲平衡。
- **短板要主动说**：无自动评估 → LLM-as-Judge + 评测集。
- **万能三段式**：问题 → 方案 → 权衡与改进。
